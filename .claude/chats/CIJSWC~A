  请基于文档                                                                
  .claude/context/libarm96_binary_mods_and_risk_control_2026-05-03.md
  开始执行“原始二进制对照评估”。                                            
  约束：只做评估，不改代码、不改脚本、不提交。              
  请按 Workstream A→E 顺序推进，先完成                                      
  A（样本固定与结构/内容差异基线），输出：                                  
  1. 已确认样本清单（含 hash/build-id）；                                   
  2. libarm96 改写热区图（按段/偏移归类）；                                 
  3. 首批 P0 风险（最多 5 条，含触发条件与影响）。          
  每完成一个 Workstream 给我阶段结论，再进入下一个。  

  
如果需要可以借助反编译环境：
可以借助ida-mcp-server来反编译，服务地址：http://192.168.11.26:8745/mcp
默认打开文件与当前课题无关，你需要指定打开的文件，接口是支持的
